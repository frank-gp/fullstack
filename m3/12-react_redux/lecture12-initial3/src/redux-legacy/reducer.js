const initialState = {}

export const reducer = (state = initialState, action) => {

    switch (action.type) {
        case "ADD_TYPE":
            // logica
            return state
        default:
            return state
    }
    
}